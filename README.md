# Working with organization data

## TL;DR

  - Install python ([mise](https://github.com/jdx/mise) setup is included for python)
  - Clone the repository
  - Navigate to the project directory
  - Run `pip install jupyter requests pandas rdflib pyld ipywidgets openpyxl`
  - Run `jupyter lab` or `python3 -m jupyterlab`



